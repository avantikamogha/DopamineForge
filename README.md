### 🧠 The Forge Engine (v1.0)
- **Work Tasks**: Contain `energy_status`. If "Suboptimal", the UI should show a warning ⚠️.
- **Break Tasks**: Contain `break_details`. Use `item.break_details.icon` for the display emoji and `item.break_details.activity` for the title.